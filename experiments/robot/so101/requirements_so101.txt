# Install this file in a Python 3.12+ SO-101 robot-client environment, separately from the GPU training
# environment (do not `pip install -e .` here -- see SO101.md).
#
# lerobot>=0.6.0 requires Python>=3.12 and uses the top-level lerobot.robots/lerobot.cameras API (the pre-0.6
# lerobot.common.* namespace is gone). so101_utils.py/run_so101_eval.py already target this API (dotted
# "{motor}.pos" observation/action keys), so do not pin lerobot back below 0.6.
#
# The [feetech] extra pulls in feetech-servo-sdk (import name scservo_sdk), the motor driver SO101Follower needs
# to actually talk to the servos. Without it, connect() fails at runtime even though lerobot itself imports fine.
lerobot[feetech]>=0.6.0,<0.7.0
# lerobot 0.6.x's own core dependency pins (mirrored here so the resolver doesn't have to guess, and so a stale
# pin here can't fight lerobot's own requirement the way the old draccus==0.8.0 pin did):
numpy>=2.0.0,<2.3.0
draccus>=0.11.6,<0.12.0
# Only used by vla_client_utils.py::resize_image_for_policy, to exactly reproduce the server's training-time
# resize/JPEG pipeline. tensorflow<2.16 has no Python 3.12 wheel.
tensorflow>=2.16
imageio[ffmpeg]
requests
json_numpy
